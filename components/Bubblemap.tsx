const Bubblemap = () => {
  return (
    <div>
      <input
        type="text"
        // Aqui probablemente es donde colocaremos variable que haga el map
        placeholder={'Marcas y tiendas'}
        className="border border-GRAY rounded-full h-11 text-center text-xs font-medium"
      />
    </div>
  );
};

export default Bubblemap;
